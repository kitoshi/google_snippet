export default function InstallText(params) {
  return (
    <>
      <section>
        <p>
          For more information, see Setting Up a C# Development Environment. If
          you are using Visual Studio 2017 or higher, open nuget package manager
          window and type the following: Install-Package Google.Apis If you are
          using .NET Core command-line interface tools to install your
          dependencies, run the following command: dotnet add package
          Google.Apis
        </p>
      </section>
    </>
  )
}
